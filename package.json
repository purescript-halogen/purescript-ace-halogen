{
  "private": true,
  "scripts": {
    "postinstall": "pulp dep install",
    "build": "pulp build && rimraf docs && pulp docs"
  },
  "devDependencies": {
    "pulp": "^4.4.1",
    "rimraf": "^2.4.3"
  },
  "dependencies": {
    "purescript": "^0.7.6"
  }
}
