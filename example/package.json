{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp browserify --optimise --to example.js"
  },
  "devDependencies": {
    "bower": "^1.8.4",
    "pulp": "^12.4.2",
    "purescript": "^0.12.5",
    "rimraf": "^2.6.2"
  }
}
